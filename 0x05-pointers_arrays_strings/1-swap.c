#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps the values of two different given integers.
 * @*a and *b - pointers.
 * Return: 0
 */
void swap_int(int *a, int *b)
{
	*a = b && *b = a;
}	
